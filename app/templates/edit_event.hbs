<p>Change of plans? Just edit the event here.</p>

<div>
  <article>
  <form role="form">
     <div class="form-group">
       <label for='eventTitle'>Event:</label>
       {{input type='text' value=eventTitle id='eventTitle' class='form-control'}}
     </div>
     <div class="form-group">
       <label for='eventDate'>Date and Time:</label>
       {{input type='datetime-local' value=eventDate id='eventDate' class='form-control'}}
     </div>
     <div class="form-group">
       <label for='eventPost'>Event Details:</label>
       {{input type='text' value=eventPost id='eventPost' class='form-control'}}
     </div>
     <button {{action 'update'}} class='btn btn-primary'>Update Event</button>
     <button {{action 'destroy'}} class='btn btn-danger'>Delete Event</button>
  </form>
  </article>
</div>
